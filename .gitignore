# Go
/poolpilot-relay
*.test
*.out
# coverage-gate artifacts a local dry run leaves in the tree (*.out covers the
# profiles; these two don't match it): the rendered HTML report and the tee'd
# test log.
coverage.html
test-output.txt

# local env
.env
*.local

# go.work (local multi-module dev against a sibling checkout; never committed)
go.work
go.work.sum

# Vendored Claude tooling — gitignored, do not commit
.claude/

# Local-only mistakes log (the "log mistakes in MISTAKES.md" convention) — kept
# out of this public repo; the running log stays on each maintainer's machine.
MISTAKES.md
