# Go
/poolpilot-relay
*.test
*.out

# local env
.env
*.local

# go.work (local multi-module dev against a sibling checkout; never committed)
go.work
go.work.sum

# Vendored Claude tooling (canonical in poolpilot-claude) — local-only, never committed here.
.claude/
